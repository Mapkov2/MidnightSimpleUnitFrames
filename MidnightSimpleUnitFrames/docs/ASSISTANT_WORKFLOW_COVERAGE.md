# Assistant Workflow Lifecycle Coverage

This matrix tracks stateful Assistant workflows beyond raw settings coverage.

| Workflow / action family | Start | Confirm/apply | Cancel | Exit/stop | Toggle | Status/diagnostic | Existing helper used | Blocked / TODO reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MSUF Edit Mode (`assistant.action.editMode.enter`) | yes | yes | yes | yes | yes | yes | `M.SetMSUFEditModeActive`, `M.CancelMSUFEditMode`, `M.ToggleMSUFEditMode`; backed by direct Edit Mode helpers when available | Complete for shared MSUF Edit Mode helpers. If helper exports are unavailable at runtime, Assistant reports the missing enter, exit, or cancel helper. |
| Profile import into active profile | yes | yes | yes | n/a | n/a | yes | `MSUF_ImportFromString`, Assistant confirmation queue, large text import panel | Complete for active-profile import. Cancel is handled by confirmation cancel or closing the import panel. |
| Profile import into new profile | yes | yes | yes | n/a | n/a | yes | `MSUF_CreateProfile`, `MSUF_SwitchProfile`, `MSUF_ImportFromString`, profile snapshot undo | Complete for create/import/rollback failure paths. |
| Profile create/copy/delete/switch/rename | yes | yes for destructive/copy/rename | yes for destructive/copy/rename | n/a | n/a | yes | `MSUF_CreateProfile`, `MSUF_CopyProfile`, `MSUF_DeleteProfile`, `MSUF_SwitchProfile`, `MSUF_RenameProfile`, profile snapshot undo | Complete for public profile helpers. Source-profile copy and rename pending flows show and accept natural destination replies such as `call it Raid Backup`, `to Raid Renamed`, and German `in Raid Neu` plus natural cancels. Default profile rename is intentionally rejected; copy it instead. |
| Profile export/Wago link | yes | n/a | n/a | n/a | n/a | yes | `MSUF_ExportSelectionToString`, Assistant copy/link panel, Wago profile link action, profile staging selector-state action | Complete when export helper exists. Export kind/string fields are also stageable through the Assistant. |
| Spec profile routing | yes | yes | n/a | n/a | enable/disable auto-switch | yes | Spec auto-switch helpers or DB fallback; `MSUF_SetSpecProfile` or DB fallback | Complete for assignment and clear actions when spec/profile data is available. |
| Factory reset all | yes | yes | yes | n/a | n/a | via status/TODO | `M.StageFactoryReset`, Assistant confirmation queue | Complete for start/confirm/cancel. Reload remains a user/system action after staging. |
| Unit copy-to | yes | yes | yes/implicit | n/a | n/a | via action response | `M.UnitPage.CopyUnitSettings`, Assistant confirmation queue, Unit Copy category selector-state action | Single-command source/destination copy is covered, including multi-category scoped phrases and popup category staging state. |
| Group frame copy-to | yes | yes | yes/implicit | n/a | n/a | via action response | `M.GroupPage.CopyGroupSettings`, Assistant confirmation queue, Group Copy category selector-state action | Single-command Party/Raid/Mythic Raid copy is covered, including multi-category scoped phrases, Group Aura-only copy such as `copy raid auras to party`, negative Aura exclusions such as `without auras`, and popup category staging state. |
| Assistant history and menu history | yes | yes | n/a | n/a | yes | via action response | `A.PushUndo`, `A.UndoLast`, `A.RedoLast`, `A.RememberAppliedBundle`, `M.Undo`, `M.Redo`, `M.ResetHistorySession`; parser special-cases plain Assistant `undo`/`redo` and explicit menu-history commands | Confirmation prompts accept natural apply/cancel replies while preserving the confirmation gate. Assistant setting changes and snapshot-backed actions offer a direct `undo`/follow-up hint after success. `what did you change` / `what did you do` explains either the last setting value or the last executed Assistant action result. Assistant undo/redo and native MSUF menu-history undo/redo/session reset are covered. Session reset is confirmed through the Assistant and uses the same helper as Shift-click Undo. |
| Assistant background jobs / combat guard | deferred | resumes after combat | n/a | n/a | n/a | yes | `A.StartJob`, `A._RunJobPump`, `A.ResumeCombatDeferredJobs`, `PLAYER_REGEN_ENABLED`; covered by `tools/assistant_dashboard_smoke.lua` | Assistant background jobs do not execute steps or schedule pump slices while combat-locked. Jobs started in combat are parked, existing jobs pause if combat starts before a slice, performance warmup is rejected in combat, UI refresh/busy pulse/NoMatch telemetry/perf trace writes are blocked, and parked work resumes after `PLAYER_REGEN_ENABLED`. |
| Gameplay helpers | yes | yes | n/a | n/a | preview toggles where exposed | yes | Gameplay DB helpers, TotemFrame preview/reset, Crosshair spell helper, Edit Mode helper, page-context routing, focused Gameplay helper diagnostic | AdvancedGameplay rows are currently closed. Dashboard-path smoke covers Combat Timer, Combat Enter/Leave, TotemFrame, First Dance, and Crosshair controls plus focused Gameplay diagnostics such as Combat Timer enable repair and Crosshair missing-spell explanation; Crosshair position is intentionally reported as unavailable because no UI/DB offset exists. |
| Class Resource helpers | yes | yes | n/a | n/a | quick setup where exposed | yes | ClassPower DB helpers, quick setup action, diagnostic action, page-context routing | Class Resources rows are registered; Dashboard-path smoke covers movement, width mode, background opacity, prediction, Alternative Mana height, quick setup, and setting-backed diagnostic suggested fixes. |
| Registry exact-alias intent metadata | yes | yes | n/a | n/a | n/a | via parser and perf-budget smokes | `P.ParseRegistryExactAliasShortcut`, setting `exactAliases`, `moveAxis`, `moveStep`, `intentGuard` metadata | Deterministic natural phrases can now resolve before Geometry specialty parsers without full fuzzy scans. Detached Power Bar X/Y movement is covered through registry exact aliases, direction-aware relative deltas, and an attached-bar guard that preserves the user-facing failure when the bar is not detached. `tools/assistant_perf_budget_smoke.lua` now fails if hot exact-alias, Aura, compound, or location routes exceed conservative average-ms budgets. |
| Group frame helpers | yes | yes | n/a | n/a | preview toggles where exposed | yes | Group frame DB helpers, status icon preview/reset, selector-state helpers, custom anchor start/clear/direct frame-name control | Layout/Bars/Indicators are broadly covered, including class color mode for Raid/Party/Mythic Raid, anchor target, growth direction, frame scaling/manual scale/breakpoint scale controls, backdrop/HP opacity sliders, selected-item UI selectors, individual text movement plus exact X/Y offset, left/center/right text-slot dropdown/offset, text-layer values, natural phrases like `move party frame name down`, `move party hp left text down 3`, and `move raid right power label up 2`, text move-together editor state including `individual text units` wording, custom anchor workflow/direct target control, and Cooldown Manager anchor wording that distinguishes concrete unit/group frame requests from unsafe broad-frame requests. |
| Global appearance helpers | yes | yes | n/a | n/a | preview/test toggles where exposed | yes | Global Bars/Fonts scoped settings, ONLY override parser, Global Castbars helpers, castbar preview helper, highlight/dispel test helpers, color reset helpers | Global Fonts, Global Bars, and Global Castbars rows are closed. |
| Guided setup | yes | n/a | yes | finish | n/a | yes | Assistant guided setup context (`A.guidedSetup`) | Start/next/back/skip/show/finish/cancel are covered for the general setup and branch-specific non-Aura guides for Group Frames, Castbars, Profiles, Class Resources, Gameplay helpers, and Bars/Fonts/Colors. |
| Dashboard panels | yes | n/a | yes | n/a | toggle | yes | `M.PersistMenuStateValue`, `M.Open` / `M.SelectPage`, `A.Workflow.SetDashboardPanel`, `MSUF_NS.MSUF_Changelog` | Recovery, Scaling, and Changelog disclosure headers are counted in the matrix and controllable through the Assistant. Release-note questions are also answered from the bundled changelog data through the Knowledge path. |
| Dashboard navigation pages | yes | n/a | n/a | yes for Assistant and native page history | NavRail sections | yes | `M.Search.OpenResults`, `M.SearchBridge.OpenSearchTarget`, `M.Open`, `M.SelectPage`, `A.Workflow.PushNavigationPage`, `A.Workflow.GoBackPage`, `M.GoBackPage`, `M.GoForwardPage`, `M.GetPageHistoryState`, `M.SetNavHeaderOpen`, `M.SetSearchIntroSeen` | Page-open/search-target coverage is broad, including visible page-label shortcuts such as Pet and Target of Target. Public Search result openings submit to the Assistant first, while native Search rendering remains an internal/fallback path. NavRail section disclosure, search-intro state, Assistant page-stack back, and native Menu2 page back/forward history are registered. |
| Menu2 window shell | yes | n/a | n/a | close | minimize/maximize/restore | via action response | `M.HideSlashMenuAndMinibar`, `M.MinimizeSlashMenuWindow`, `M.MaximizeSlashMenuWindow`, `M.RestoreSlashMenuWindow`, `M.RestoreMinimizedSlashMenu` | Complete for the visible window and minimized-bar shell buttons. |
| Diagnostics | yes | n/a | n/a | n/a | n/a | yes | Assistant diagnostic actions and shared status helpers | Unitframe, group-frame, castbar, class-resource, Aura visibility/filter, and focused Gameplay-helper diagnostics offer setting/action-backed suggested fixes where a concrete helper is disabled, hidden, filtered, blacklisted, or mis-sized; Aura diagnostics cover disabled Unit Auras, hidden unit Aura lanes, shared Buff/Debuff toggles, unit exclusive/player/raid filters, custom ignore lists, unit blacklist entries with clear-all repair, group Aura lane ownership, max-icon zero, group filter tokens, category blacklist review, and clear-all category blacklist repair. Profile diagnostics can offer action-backed numbered next steps such as opening Profiles or clearing broken spec-profile mappings; Dashboard setup diagnostics can offer Recovery Tools and pending-workflow cancel choices; single-choice prompts accept natural replies such as `yes` / `open it`, and unambiguous real repairs accept natural follow-ups such as `fix it` / `mach das` even when a separate navigation choice is also shown. Status, scoped help, and Edit Mode status are covered. Spell-name Aura blacklist edge cases, more granular category repair wording, and branch-specific repair troubleshooters remain TODO as new helpers appear. |
| NoMatch learning review | yes | n/a | clear with confirmation | n/a | n/a | yes | `A.RecordNoMatch`, `A.AnalyzeNoMatchText`, `A.NoMatchTelemetryText`, `A.NoMatchWorklistText`, `assistant_nomatch_telemetry`, `assistant_nomatch_worklist`, `assistant_nomatch_clear` | Unknown wording is stored out of combat only, grouped by phrase/count/source/status, classified into owner/tags, assigned a priority and candidate class, and shown as review candidates for alias, generic intent, action workflow, media alias, Aura backend, or Knowledge/help follow-up work. When the full registry/parser is loaded, registry-like misses also show scored concrete setting keys, a concrete per-phrase `learningPlan`, an `Alias/intent plan` section, and TSV columns for `registryCandidates` plus `learningPlan`. Worklists can be filtered by owner phrases such as registry aliases, anchors, Auras, Knowledge, media, or actions. Clearing the telemetry is confirmed. |

Coverage counters from the offline harness after this pass:

- Settings: 2901
- Actions: 142
- UI matrix rows: 662 (`662` registered, `0` partial, `0` documented TODO, `0` unmapped by strict static UI-to-registry mapping).
- Registered actions: 142
- Partial/todo/unmapped matrix rows: 0
- Lifecycle-covered workflows: Edit Mode, profile import/export/profile ops and profile staging fields, spec profile routing plus broken-mapping repair, factory reset, unit copy-to and copy-category staging, group frame copy-to including Group Aura copy and copy-category staging, Assistant history, Assistant background-job combat guard, NoMatch learning review, registry exact-alias intent metadata, gameplay helpers including page-context controls and focused diagnostics, Class Resource helpers including quick setup/page-context controls, group frame helpers including custom anchor start/clear/direct frame-name control, global appearance helpers, Menu2 selector-state helpers including text move-together mode, general and branch-specific guided setup, Dashboard panels/navigation including Assistant and native page-history back/forward, NavRail disclosure/search-intro state, Menu2 window shell controls, and diagnostics including profile/gameplay/class-resource/dashboard setup plus Aura visibility/filter diagnostics.
- Focused parser smoke: `lua tools\assistant_parser_smoke.lua` covers Pet/Target of Target page navigation, page-label shortcuts, Dashboard page-back/page-forward actions, Dashboard panel open/close/toggle actions, NavRail section/search-intro actions, Menu2 shell close/minimize/maximize/restore actions, native MSUF menu history undo/redo/session-reset actions, general/scoped help aliases, short profile-copy aliases, explicit profile-copy workflow start, profile rename aliases, profile staging fields, Unit Copy and Group Copy category staging, unit and group copy category scopes, global UI scale presets, guided setup start/step/cancel, support links, Wago backup confirmation, Edit Mode lifecycle/status, diagnostics including profile/gameplay/class-resource/dashboard setup diagnostics, last-change replay for Unit/Group/scoped Bars targets including color values, factory reset staging, natural reset phrases, Bars/Fonts ONLY scoped override routing, group-frame class color mode for `raidframe` and generic `group frames`, Group Layout anchor target, natural growth wording, manual frame scale, numbered breakpoint scale commands, and backdrop/HP opacity sliders, direct unit/group custom anchor frame-name commands, individual unit/group name/HP/power text movement including `health`/`hp`/`mana`, `party frame name`, plural names, and label shorthand, exact unit/group text X/Y offset slider values, left/center/right HP/Power text-slot dropdown and offset commands, Name/HP/Power text-size and text-layer slider commands with explicit and current-page scope, HP/Power text-anchor/alignment/put-on-left-right aliases mapped to visible slot selectors, exact RGB/hex values for registered color pickers, dark-mode bar-color slider phrasing including exact percent and relative darker/lighter commands, Class Resource movement/width-mode/background-opacity/prediction/Alternative-Mana commands, Gameplay root toggles, page-local Combat Timer commands, Combat Enter/Leave text/size/duration/movement, TotemFrame enable/size/movement/anchors, First Dance enable/size/movement/ready toggle, Crosshair size/thickness/range-color/per-spec/spell commands and explicit Crosshair-position unknown handling, Menu2 selector-state actions including text move-together mode and `individual text units` phrasing, scoped bar border color/texture aliases, relative numeric registry changes, unit detail pixel moves, unit/group opacity shortcuts, root castbar toggle, unit castbar spell-name/text/icon/time/interrupt/channel-tick/glow/spark/latency/unified-direction toggles, castbar text color, castbar spell-text offset, and Pet frame movement.
- Dashboard-path smoke: `lua tools\assistant_dashboard_smoke.lua` covers the real Assistant input path through `A.Submit`, including setting mutation/apply callbacks, natural confirmation/cancel replies such as `go ahead` and `never mind`, profile copy/rename pending-flow destination replies such as `call it Raid Backup` and `to Raid Renamed`, success responses that explain changed values and offer `undo`/follow-up next steps for single/bulk setting changes and snapshot-backed copy actions, follow-up explanations for both setting changes and the last copy action via `what did you change` / `what did you do`, German/English natural requests such as `spieler name aus`, `spieler frame einschalten`, `verschiebe spieler lebensanzeige runter 4`, `verschiebe ziel energie hoch 2`, `setze spieler breite auf 222`, `ziel zauberleiste ausschalten`, and `oeffne spieler`, branch-specific guided setup flows such as Group Frames and Castbars setup without automatic navigation, context follow-up such as `turn it back on`, current-page bare-command routing for Player, Group Health & Text `gfScope`, and Gameplay page commands such as `turn on timer`, `move timer down 5`, and `set timer anchor to target`, current-page unit/group text movement such as `move health down 4`, `move hp down 4`, `move party frame name down`, `move party hp left text down 3`, and `move raid right power label up 2`, current-page exact text-offset values such as `set hp y offset to -8`, current-page left/center/right text-slot dropdown and offset mutation, current-page Name/HP/Power text-size and text-layer slider mutation, current-page group opacity mutation, Class Resources page-context mutation such as `set width mode to custom`, `set background opacity to 40`, and `turn off prediction`, Gameplay mutation and diagnostics for Combat Timer, Combat Enter/Leave, TotemFrame, First Dance, and Crosshair controls, conversation-context routing for bare `turn off frame` follow-ups on Player and Raid, last-change replay such as `same for target`, `same for raid`, `do that for focus`, and `same for party`, Assistant page-stack back navigation through `open player` -> `open target` -> `back`, native Menu2 page-history back/forward after direct menu navigation, native menu history undo/redo/session-reset helper calls, profile diagnostic action choices, dashboard diagnostic action choices, class-resource/dashboard setup diagnostics, group-frame class color mode mutation, Group Layout anchor target, natural growth wording, manual frame scale, numbered breakpoint scale mutation, and backdrop/HP opacity slider mutation, direct custom anchor frame-name mutation, individual unit/group name/HP/power text movement and exact X/Y text-offset slider mutation, HP/Power text-anchor/alignment/put-on-left-right slot selector routing, Unit Copy and Group Copy category-state mutation, scoped bar color picker RGB mutation and color replay to another scope, dark-mode bar-color slider mutation including exact percent and relative darker/lighter commands, profile staging-field mutation, Menu2 selector-state mutation including text move-together mode and `individual text units` phrasing, confirmation cancel, NavRail section/search-intro state, confirmed profile rename through `MSUF_RenameProfile`, numbered/worded ambiguity resolution (`option 1`, `first`), public Search result openings routed back through `A.Submit`, Knowledge/Search/FAQ answers including German profile help and no-match search responses, help/current-page fallback, and conversation history.
- Changelog Knowledge smoke: `lua tools\assistant_dashboard_smoke.lua` covers version-specific and German release-note questions such as `what changed in 6.0 Preview 2`, `was ist neu in preview 2`, and section-filtered `what changed in edit mode in latest release`.
- Parser performance budget smoke: `lua tools\assistant_perf_budget_smoke.lua` covers 12 hot parser routes with fixed average-ms budgets for Group Aura exact aliases, Unit Aura offsets, shared Aura settings, compound chains, Detached Power Bar generic movement, Aura actions, and location/Knowledge routing.

Current largest workflow gaps:

- Auras and Group Auras have static UI-to-registry parity, Group Aura copy, setting/action-backed visibility/filter diagnostics, Unit Aura blacklist add/remove/show/clear workflows, and Group Aura category-blacklist clear-all repair now; they still need richer spell-name blacklist resolution, more granular category repair wording, live in-game phrase validation, and regular NoMatch-driven alias hardening.
- The legacy Dashboard command-center fallback is statically present but not the normal user-facing path when `MSUF.Assistant.BuildDashboardCard` is loaded; it is now mapped to the Assistant Dashboard route and covered by Dashboard-path smoke tests.
- Remaining transient UI staging fields are not open matrix gaps in the current inventory. Menu2 selector state for visible tabs/dropdowns/editor slots and Unit/Group copy category popup state is controlled through `set_menu_selector_state`.
- Search/FAQ/changelog remains internally backed by existing SearchData catalogs and bundled `MSUF_NS.MSUF_Changelog` data, but user-facing Help/Search/FAQ/release-note questions and public Search result openings are now routed through the Assistant Knowledge path first.
